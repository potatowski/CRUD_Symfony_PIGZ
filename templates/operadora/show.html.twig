{% extends 'base.html.twig' %}

{% block title %}Operadora{% endblock %}

{% block body %}
    <h1>Operadora</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ operadora.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ operadora.nome }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('operadora_index') }}">back to list</a>

    <a href="{{ path('operadora_edit', {'id': operadora.id}) }}">edit</a>

    {{ include('operadora/_delete_form.html.twig') }}
{% endblock %}
